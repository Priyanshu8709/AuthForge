PORT=5000
MONGODB_URI=your_mongodb_uri
JWT_SECRET=your_jwt_secret
# use Brevo (Sendinblue) transactional email service
BREVO_API_KEY=your_brevo_api_key
# optional sender address to appear in emails
EMAIL_FROM=your_verified_sender@example.com
CLIENT_URL=http://localhost:5173
# Optional: allow dynamic preview URLs (example for Vercel preview deployments)
# CLIENT_URL_REGEX=^https://auth-forge-.*-priyanshu8709s-projects\\.vercel\\.app$
NODE_ENV=development
